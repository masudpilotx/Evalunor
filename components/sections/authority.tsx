import { Check } from "lucide-react";
import { Reveal } from "@/components/ui/reveal";
import { Section } from "@/components/ui/section";

export function Authority() { return <Section bordered><div className="grid gap-12 lg:grid-cols-[.85fr_1fr] lg:gap-24"><Reveal><p className="kicker mb-5">The real problem</p><h2 className="display max-w-xl text-[clamp(2.4rem,5vw,4.1rem)]">Your reputation deserves more than <em className="font-normal text-primary">page two.</em></h2></Reveal><Reveal delay={80}><div className="max-w-xl text-muted-foreground"><p className="text-xl leading-relaxed text-foreground">Your work is exceptional. But an invisible search presence makes you look average and unproven. Buyers choose the competitor they found first, and you never hear about the deal you lost.</p><p className="mt-6 leading-relaxed">The fix is not more blog posts. It is a system: a site search engines can crawl and trust, content built around buying intent, authority earned instead of rented, and reporting tied to revenue.</p><ul className="mt-7 grid gap-3">{["Senior operators only, no account-manager telephone game", "One roadmap, prioritised by revenue impact", "Reporting you can take straight into a board deck"].map((item) => <li key={item} className="flex gap-3 text-sm"><Check className="mt-0.5 size-4 shrink-0 text-primary" />{item}</li>)}</ul></div></Reveal></div></Section>; }
