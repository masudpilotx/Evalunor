import { Star } from "lucide-react";
import { Section } from "@/components/ui/section";
import { SectionHeading } from "@/components/ui/section-heading";
import { Reveal } from "@/components/ui/reveal";
import { testimonials } from "@/lib/data/testimonials";
export function Testimonials() { return <Section bordered><SectionHeading kicker="In their words" title={<>Clients who stopped <em className="font-normal text-primary">guessing.</em></>} /><div className="mt-12 grid gap-5 md:grid-cols-2">{testimonials.map((item, index) => <Reveal key={item.author} delay={index * 80}><figure className="rounded-lg border border-border bg-card/50 p-7 sm:p-8"><p className="font-mono text-[10px] uppercase tracking-[.15em] text-muted-foreground">{item.service}</p><blockquote className="mt-5 text-[17px] leading-relaxed tracking-tight">“{item.quote}”</blockquote><figcaption className="mt-7 flex items-center gap-3 border-t border-border pt-5"><span className="grid size-10 place-items-center rounded-full bg-primary text-xs font-bold text-primary-foreground">{item.initials}</span><span className="flex flex-1 flex-col text-sm"><strong>{item.author}</strong><em className="mt-0.5 text-xs not-italic text-muted-foreground">{item.meta}</em></span><span className="inline-flex items-center gap-1 text-xs font-semibold text-primary"><Star className="size-3.5 fill-primary" />{item.score}</span></figcaption></figure></Reveal>)}</div></Section>; }
