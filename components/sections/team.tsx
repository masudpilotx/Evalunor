import { ArrowUpRight } from "lucide-react";
import Link from "next/link";
import { Reveal } from "@/components/ui/reveal";
import { Section } from "@/components/ui/section";
import { SectionHeading } from "@/components/ui/section-heading";
import { team } from "@/lib/data/team";

export function Team() { return <Section id="about" className="bg-card/40" bordered><SectionHeading kicker="About Evalunor" title={<>Senior minds. <em className="font-normal text-primary">Serious search.</em></>} description="The people behind the rankings. Three specialists, one standard: work that earns attention and keeps it." /><div className="mt-12 grid gap-5 lg:grid-cols-3">{team.map((member, index) => <Reveal key={member.name} delay={index * 80}><article className="group overflow-hidden rounded-lg border border-border bg-background transition hover:-translate-y-1 hover:border-primary/50"><div className="relative aspect-[4/5] overflow-hidden bg-secondary"><img src={member.photo} alt={`${member.name}, ${member.role} at Evalunor`} loading="lazy" decoding="async" className="h-full w-full object-cover transition duration-700 group-hover:scale-105" style={{ objectPosition: member.objectPosition }} /><div className="absolute inset-x-0 bottom-0 h-1/3 bg-gradient-to-t from-background to-transparent" /></div><div className="p-6"><span className="font-mono text-[10px] uppercase tracking-[.18em] text-primary">{String(index + 1).padStart(2, "0")} / {member.discipline}</span><h3 className="mt-3 font-display text-xl font-semibold leading-tight tracking-tight">{member.name}</h3><p className="mt-1 text-sm font-medium leading-snug text-foreground/75">{member.role}</p><p className="mt-5 border-t border-border pt-4 text-sm leading-relaxed text-muted-foreground">{member.bio}</p><ul className="mt-4 flex flex-wrap gap-1.5">{member.focus.map((skill) => <li key={skill} className="rounded-full border border-border px-2.5 py-1 text-[10px] text-muted-foreground">{skill}</li>)}</ul></div></article></Reveal>)}</div><Reveal><div className="mt-14 grid gap-8 border-t border-border pt-10 md:grid-cols-[1fr_1fr]"><h3 className="display text-3xl sm:text-4xl">We do not chase rankings.<br /><em className="font-normal text-primary">We build relevance.</em></h3><div className="grid gap-5 text-sm text-muted-foreground"><p><strong className="mr-2 text-foreground">01. Senior ownership</strong> The person shaping your strategy is involved in the execution. Every week.</p><p><strong className="mr-2 text-foreground">02. Clear thinking</strong> We explain the why, the trade-offs and expected impact before anything ships.</p><p><strong className="mr-2 text-foreground">03. Compounding work</strong> We prioritise assets and systems that get stronger over time.</p></div></div></Reveal><div className="mt-8"><Link href="/about" className="inline-flex items-center gap-2 text-sm font-semibold text-primary hover:underline">Meet the full team <ArrowUpRight className="size-4" /></Link></div></Section>; }
